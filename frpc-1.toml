# Example only — install_coti-full-node.sh overwrites this on --with-frp / --frpc-enabled=true.
serverAddr = "virginia.fullnode.mainnet.coti.io"
serverPort = 7000

[[proxies]]
name = "replace-me.fullnode.mainnet.coti.io"
type = "http"
localIP = "nginx-frpc-gateway"
localPort = 8080
customDomains = ["replace-me.fullnode.mainnet.coti.io"]
